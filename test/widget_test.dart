/*
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:remedy_mate/main.dart';

void main() {
  testWidgets('MyApp builds and shows MaterialApp', (
    WidgetTester tester,
  ) async {
    await tester.pumpWidget(const MyApp());

    // Check that MaterialApp is present
    expect(find.byType(MaterialApp), findsOneWidget);

    // Optionally, check for the app title in the widget tree
    expect(
      find.text('ChatBot'),
      findsNothing,
    ); // Title is not shown as a widget by default

    // You can add more checks here if your home widget displays something specific
  });
}
*/
